name: CI/CD for Fullstack App

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Login to Github Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.CHACO_TOKEN }}
    
    - name: Build image and push
      run: |
        docker compose -f Chacobache_API/docker-compose-dev.yml build
        docker compose -f Chacobache_API/docker-compose-dev.yml push
    
  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      # 1. Clona el repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Loguéate en el GitHub Container Registry
      - name: Login to Github Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CHACO_TOKEN }}

      # 3. Despliega el contenedor
      - name: Deploy Docker container
        run: |
          docker-compose -f Chacobache_API/docker-compose-dev.yml pull
          docker-compose -f Chacobache_API/docker-compose-dev.yml up -d


#    - name: Setup Node.js
#      uses: actions/setup-node@v3
#      with:
#        node-version: '18'

#    - name: Install dependencies for backend
#      working-directory: Chacobache_API
#      run: npm install

#    - name: Run backend tests
#      working-directory: Chacobache_API
#      run: npm test

#    - name: Setup Chromium
#      uses: browser-actions/setup-chromium@v1

#    - name: Install dependencies for frontend
#      working-directory: Chacobache
#      run: npm install

#    - name: Run frontend tests
#      working-directory: Chacobache
#      run: npm test

#    - name: Build and Deploy Frontend
#      working-directory: Chacobache
#      run: npm run build

